import { ForbiddenCarDirective } from './forbidden-car.directive';

describe('ForbiddenCarDirective', () => {
  it('should create an instance', () => {
    const directive = new ForbiddenCarDirective();
    expect(directive).toBeTruthy();
  });
});
